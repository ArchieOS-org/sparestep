# sparestep-managed-skill: v1
interface:
  display_name: "Sparestep"
  short_description: "Finish the task and save worthwhile side work"
  default_prompt: "Use $sparestep to keep this task focused."

policy:
  allow_implicit_invocation: false
